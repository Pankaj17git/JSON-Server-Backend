{
  "name": "json-server-backend",
  "version": "1.0.0",
  "description": "Mock API using JSON Server",
  "scripts": {
    "start": "json-server --watch db.json --port 10000"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
