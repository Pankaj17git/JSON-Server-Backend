{
  "name": "json-server-backend",
  "version": "1.0.0",
  "description": "Mock API using JSON Server",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch user.json --port 4001"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
